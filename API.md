# API — منصة الطارق التعليمية

REST API عام للتطبيقات الخارجية (تطبيقات الجوال، تكاملات، لوحات معلومات).

## المصادقة

جميع النقاط تتطلب Bearer Token يُنشأ من `/admin/api`:

```http
Authorization: Bearer tk_xxxxxxxxxxxxxxxx
```

## القاعدة (Base URL)

```
https://<your-domain>/api/public/v1
```

## النقاط المتاحة

### GET `/stats`
إحصائيات عامة للمنصة.
```json
{ "students": 120, "exams": 34, "attempts": 890, "server_time": "..." }
```

### GET `/students`
قائمة الطلاب النشطين (غير المؤرشفين).
- `limit` (اختياري، افتراضي 50، حد أقصى 200)
- `grade` (اختياري)

### GET `/leaderboard`
ترتيب الطلاب حسب النقاط.
- `limit` (اختياري، افتراضي 20، حد أقصى 100)

## أكواد الاستجابة
- `200` نجاح
- `401` مفتاح مفقود/غير صالح/مُبطل/منتهي
- `500` خطأ داخلي

## ملاحظات أمان
- خزّن المفاتيح في مكان آمن (Environment variables).
- استخدم `expires_at` لتقييد صلاحية المفاتيح.
- عند التسريب: أبطل المفتاح فوراً من لوحة `/admin/api`.
